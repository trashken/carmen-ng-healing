---
import Layout from '../../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import ServiceDetail from '../../../components/ServiceDetail.astro';
import { getDictionary } from '../../../i18n';

export async function getStaticPaths() {
  const all = await getCollection('services');
  return all
    .filter((entry) =>
      entry.data.intro?.en !== '' ||
      entry.data.serviceBody?.en !== '' ||
      entry.data.offerings.length > 0
    )
    .map((entry) => ({
      params: { slug: entry.id.replace(/\.md$/, '') },
      props: { entry },
    }));
}

const { entry } = Astro.props;
const t = getDictionary('jp');
const title = entry.data.title.jp;
const description = entry.data.desc.jp;
---

<Layout title={`${title} | ${t.meta.siteName}`} description={description} locale="jp">
  <article class="max-w-6xl mx-auto px-6 py-12">
    <ServiceDetail locale="jp" entry={entry} />
  </article>
</Layout>
